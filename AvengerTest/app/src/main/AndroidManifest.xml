<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jorgecaseda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_avenger"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingActivity"></activity>
        <activity android:name=".SeventhQuestionActivity" />
        <activity android:name=".SixthQuestionActivity" />
        <activity android:name=".FifthQuestionActivity" />
        <activity android:name=".FinalActivity" />
        <activity android:name=".FourthQuestionActivity" />
        <activity android:name=".ThirdQuestionActivity" />
        <activity android:name=".SecondQuestionActivity" />
        <activity android:name=".FirstQuestionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>